#[tokio::main]
async fn main() {
    // database_init();
    // let mut  ctx = CTX.get_mut().unwrap().lock().unwrap();
    // let data = AttendanceType::select_by_id(&mut *ctx, "2".to_string()).await;
    // println!("{:#?}", data);
}
